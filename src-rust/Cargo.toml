[package]
name = "forgum-core"
version = "0.1.0"
edition = "2021"

[dependencies]
crossterm = "0.27"
clap = { version = "4.4", features = ["derive"] }
image = "0.24" # For image rendering/scaling later