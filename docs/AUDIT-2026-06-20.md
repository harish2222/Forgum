# Forgum Full Codebase Audit — 2026-06-20

## CRITICAL (Fix Immediately)
1. `Get-HelpMessage.ps1:573` — `'h'` alias maps to `'version'` instead of `'root'` (help)
2. `Invoke-ForgumRun.ps1:30-36` — Double output: animation renders + pipeline return
3. `Read-CowFile.ps1:24-26` — `$CustomPath` has no path traversal validation
4. `forgum.ps1:96-97` — Dead code: pattern blocks unreachable after `default`
5. `Forgum.psm1:182-227` — Tab completion for non-existent params (Action, Cow, CowFile, PreviewCow, Preset, Mode)
6. `Set-Forgum.ps1:28` — Stale ValidateSet missing flagship modes (aurora, plasma, ember, etc.)
7. `Bounce.ps1:75` — `IndexOf` used instead of loop index variable
8. `Blink.ps1:52-53` — Blink never fires when `$BlinkRate >= 0.5` (modulo 2 can't equal 3)
9. `Invoke-Engine.ps1:70-80` — Background process never disposed (resource leak)
10. `Invoke-ForgumExport.ps1:19,43` — Path traversal in `--output`

## HIGH (Fix Soon)
11. 10 animation files use `"`n"` split instead of `"`r?`n"` (cross-platform risk)
12. `Write-TerminalFrame.ps1:57` — `Write-Host` in non-TTY prevents pipeline capture
13. `Invoke-ForgumGallery/History/LiveHandler/List` — Unvalidated `[int]` casts
14. `Forgum.psm1:120-131` — Config rewritten + external process spawned on every module import
15. `Set-CFConfig.ps1:39` — Predictable temp file path race condition
16. `Parse-ForgumArguments.ps1:49-53` — Known option with missing value silently dropped

## MEDIUM
17. 13 files use `Write-Host` instead of `Write-Output` (breaks pipelines)
18. `Invoke-ForgumTheme.ps1:55-61` — Theme reset only resets lolcat, not full config
19. `Set-CFCowAnimate.ps1:16` — No animation mode validation
20. `Set-CFCowEyes.ps1:30` — Silent no-op on invalid custom eyes
21. `Invoke-ForgumEyes.ps1:20` — Silent no-op on invalid preset
22. `Stop-ForgumDaemon.ps1:11` — Overly broad process kill match
23. `PhysicsCow.ps1:228-230,275-276` — Particle leak in Talk/Fire effects
24. `Talking.ps1:28` — Regex misses most cow templates
25. `Dynamic.ps1:47-49` — Dead code no-op guard
26. `Invoke-ForgumRun.ps1` — --fortune flag parsed but never used
27. `CrossPlatform.ps1:41-56` — Get-ForgumConfigPath duplicates Get-ConfigPath without env var check

## LOW
28. `PhysicsCow.ps1:92-100` — Array append in hot loop (O(n²))
29. `Disco.ps1:51-67` — Per-character HSV-to-RGB (performance)
30. `Typewriter.ps1:20-24` — Slow at 50ms/char
31. `Wiggle.ps1:41-43` — Premature settling (73% static)
32. `Procedural.ps1:76` — Snowflake spawn crash when width <= 1

---

## FIXES APPLIED (2026-06-20)

### Critical fixes
| # | File | Fix |
|---|------|-----|
| 1 | `Get-HelpMessage.ps1` | Added `'h' = 'root'` alias |
| 2 | `forgum.ps1` | Moved pattern blocks before `default` case |
| 3 | `Read-CowFile.ps1` | Added path traversal check on `$CustomPath` |
| 4 | `Invoke-ForgumExport.ps1` | Added path traversal check on `--output` |
| 5 | `Bounce.ps1` | Replaced `IndexOf` with proper loop variable |
| 6 | `Blink.ps1` | Fixed blink interval modulo (was never firing) |
| 7 | `Invoke-Engine.ps1` | Added try/finally with Dispose + WaitForExit + timeout |
| 8 | `PhysicsCow.ps1` | Moved particle cleanup outside config guard |

### High-priority fixes
| # | File | Fix |
|---|------|-----|
| 9 | `Set-Forgum.ps1` | Updated ValidateSet with all 22 animation modes |
| 10 | `Forgum.psm1` | Removed auto-update config rewrite + process spawn on import |
| 11 | `Forgum.psm1` | Fixed tab completion (was for non-existent params) |
| 12 | `Set-CFConfig.ps1` | Random temp file name to prevent race condition |
| 13 | 11 animation files | Changed `"`n"` to `"`r?`n"` for cross-platform |
| 14 | `Invoke-ForgumGallery/History/List/LiveHandler` | Added `[int]::TryParse` validation |

### Test results after fixes
| Suite | Before | After |
|-------|--------|-------|
| Core (9 files) | 89 pass, 0 fail | 282 pass, 0 fail |
| Permutations | 68 pass, 55 not run | **123 pass, 0 fail** |
| Ghost | 11 pass | 11 pass |
| Rust | 19 pass | 19 pass |
| **TOTAL** | **187 pass** | **435 pass, 0 fail** |
